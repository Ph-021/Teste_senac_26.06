'''for i in range (1, 6):
    num = float(input("Digite seus números: "))
    db = num * 2 
    tp = num * 3
    qd = num * 4    
    print(db, tp, qd)    '''

'''lista_de_nomes = ["Ana", "Lucas", "Gabriel", "Lavinia", "Thiago"]
for i in lista_de_nomes:
    print(f"Bem-Vindo(a), {i}!")'''

'''for i in range(10):
    nome = input("Qual o nome do aluno? ")
    nota1 = float(input(f"Digite a primeira nota do {nome}: "))
    nota2 = float(input(f"Digite a segunda nota do {nome}: "))

    nota_geral = (nota1 + nota2) / 2

    if nota_geral >=7.0:
        print(f"O aluno está aprovado, com a nota média de {nota_geral}")
    elif nota_geral >= 5.0:
        print(f"O aluno está de recuperação, com a nota média de: {nota_geral}")
    else:
        print(f"O aluno está reprovado, com a nota média de: {nota_geral}")'''